spring:
  application:
    name: nacos-consumer
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.99.4:8848

server:
  port: 10101

management:
  endpoints:
    web:
      exposure:
        include: "*"

